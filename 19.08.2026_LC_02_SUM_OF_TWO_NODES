------GPT FIX BUGS  WITH MY PATTERN -------
class Solution {
public:
    ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {
         ListNode* current1 = l1;
         ListNode* current2 = l2;
         int carry = 0;

         ListNode* list3 = new ListNode();
         ListNode* l3 = list3;

         while (current1 != nullptr || current2 != nullptr || carry != 0) {

            int val1 = 0;
            int val2 = 0;

            if (current1 != nullptr) {
                val1 = current1->val;
            }

            if (current2 != nullptr) {
                val2 = current2->val;
            }

            int sum = val1 + val2 + carry;

            l3->val = sum % 10;
            carry = sum / 10;

            if (current1 != nullptr) {
                current1 = current1->next;
            }

            if (current2 != nullptr) {
                current2 = current2->next;
            }

            if (current1 != nullptr || current2 != nullptr || carry != 0) {
                l3->next = new ListNode();
                l3 = l3->next;
            }
         }

         return list3;
    }
};
// FIRST APPROACH (MUCHHH  WERONG)

class Solution {
public:
    ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {
        ListNode* list3 =new ListNode ();
           ListNode* l3= list3;
         ListNode* current1 =l1;
         ListNode* current2 =l2;
         while (current1!= nullptr && current2!=nullptr){
               l3->val= current1->val +current2->val;
               if (l3->val >9){
                l3->val =0;
                l3->next->val =current1->next->val +current2->next->val +1;
                l3=l3->next;
                current1=current1->next->next;
                  current2=current2->next->next;
               }
               else{ 
               l3->val= current1->val +current2->val;
                l3=l3->next;
                current1=current1->next;
                  current2=current2->next;
               }
         }
         return l3;


    }
};


----- when i trying to fixed the bugs but still some bugs ,that code is here-----
class Solution {
public:
    ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {
         ListNode* current1 =l1;
         ListNode* current2 =l2;
         int carry =0;
         while (current1!= nullptr || current2!=nullptr || carry!=0){
            if (current1==nullptr){
                current1->val==0 ;
            }
            if (current2==nullptr){
                current2->val==0 ;
            }
               current1->val = current1->val +current2->val; +carry;
               
               if (current1->val >9){

                current1->val =0 ;
               carry=1;
                current1=current1->next;
                  current2=current2->next;
               }
               else{ 
                carry=0;
                current1=current1->next;
                  current2=current2->next;
               }
         }
         return l1 ;


    }
};




